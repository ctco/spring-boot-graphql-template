type Query {
  jokeByCategory(category: JokeCategoryInput = {category: NERDY}): Joke,
  jokeById(id: String!): Joke,
}

type Joke {
  id: String!,
  text: String!,
  categories: [JokeCategory],
}

input JokeCategoryInput {
  category: JokeCategory!
}

enum JokeCategory {
  NERDY,
  EXPLICIT,
}